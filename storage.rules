rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read and write access for all users.
    // Warning: This is insecure. You should restrict access in a real application.
    match /{allPaths=**} {
      allow read, write;
    }
  }
}
